<?xml version="1.0" encoding="utf-8" ?>

<ContentView
    x:Class="NotesPro.Controls.NoteCard"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="Root">

    <Border
        Margin="0,0,0,12"
        Padding="{StaticResource Space16}"
        BackgroundColor="{StaticResource CardBackground}"
        Stroke="{StaticResource Outline}"
        StrokeThickness="1">

        <Border.StrokeShape>
            <RoundRectangle CornerRadius="{StaticResource RadiusMedium}" />
        </Border.StrokeShape>

        <Grid
            RowDefinitions="Auto,Auto,Auto"
            ColumnDefinitions="*,Auto">

            <!-- Title -->

            <Label
                Grid.Row="0"
                Grid.Column="0"
                Style="{StaticResource TitleMedium}"
                Text="{Binding Title, Source={x:Reference Root}}" />

            <!-- Pinned -->

            <Label
                Grid.Row="0"
                Grid.Column="1"
                FontSize="16"
                Text="📌"
                IsVisible="{Binding IsPinned, Source={x:Reference Root}}" />

            <!-- Description -->

            <Label
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Margin="0,8,0,0"
                Style="{StaticResource BodyMedium}"
                MaxLines="2"
                LineBreakMode="TailTruncation"
                Text="{Binding Description, Source={x:Reference Root}}" />

            <!-- Footer -->

            <Grid
                Grid.Row="2"
                Grid.ColumnSpan="2"
                Margin="0,12,0,0"
                ColumnDefinitions="*,Auto">

                <Label
                    Grid.Column="0"
                    Style="{StaticResource Caption}"
                    Text="{Binding Date, Source={x:Reference Root}}" />

                <Label
                    Grid.Column="1"
                    FontSize="16"
                    Text="⭐"
                    IsVisible="{Binding IsFavorite, Source={x:Reference Root}}" />

            </Grid>

        </Grid>

    </Border>

</ContentView>